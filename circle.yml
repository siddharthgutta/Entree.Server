machine:
  node:
    version: 4.4.5

test:
  pre:
    - grunt compile

deployment:
  production:
    branch: production
    commands:
      - ./scripts/deploy_prod.sh
  staging:
    branch: staging
    commands:
      - ./scripts/deploy_staging.sh
